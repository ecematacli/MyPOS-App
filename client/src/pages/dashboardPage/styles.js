import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(({ spacing }) => ({
  gridContainer: {
    marginTop: spacing(10)
  },
  statsGridContainer: {
    marginTop: spacing(4)
  }
}));
