import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(({ spacing }) => ({
  dateGridContainer: {
    marginTop: spacing(1)
  },
  gridContainer: {
    marginTop: spacing(7)
  }
}));
