import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(({ spacing }) => ({
  statsGridContainer: {
    marginTop: spacing(4)
  },
  chartGridContainer: {
    marginTop: spacing(10)
  }
}));
