import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  historyPaper: {
    color: 'red'
  },
  historyTable: {
    minWidth: 500
  },
  table: {
    minWidth: 650
  }
});
