import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(({ spacing }) => ({
  dashboardContainer: {
    padding: spacing(3)
  },
  dateGridContainer: {
    marginTop: spacing(1)
  },
  gridContainer: {
    marginTop: spacing(7)
  }
}));
