version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn add --global webpack
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: public/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/*/
